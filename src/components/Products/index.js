import React from 'react';

// Importing the ProductGrid component from the Grid/Container file
import ProductGrid from './Grid/Container';

// Defining a function component called Product
function Product () {
  // Rendering the ProductGrid component
  return(
    <ProductGrid />
  );
};

// Exporting the Product component as the default export of this module
export default Product;
